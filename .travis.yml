language: python
python:
    - 2.7
    - 3.3
    - 3.4
    - 3.5
    - 3.6
before_install:
    - pip install -r requirements.txt
    - pip install -r requirements-dev.txt
install:
    - pip install .
script: make cover
after_success:
    - coveralls
